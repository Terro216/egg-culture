# Яичная Культура (Egg Culture)

Гастрономический и эстетический манифест, возводящий куриное яйцо в ранг спешалти-кофе, коллекционного вина и элитного китайского чая. Ироничный по замыслу, абсолютно серьезный в подаче.

Сайт: `https://egg.<DOMAIN>` (по умолчанию — https://egg.ilyamedve.dev)

Полное описание концепции, философии и архитектуры — в [EGG.md](EGG.md). Исторический план проекта — в [PROJECT_PLAN.md](PROJECT_PLAN.md). Механика закрытой «кладки Фонина» — в [FONIN_PLAN.md](FONIN_PLAN.md).

## Стек

- **Astro 6** — статика (SSG) по умолчанию + SSR-адаптер `@astrojs/node` (standalone) для динамических страниц (`/blog`, `/prophecy`, `/dark-side/fonin`, API-роуты). Закрытый `/dark-side/games` собирается статически и проверяет статус адепта на клиенте.
- **React 19** — точечные интерактивные острова (Island Architecture).
- **Three.js** — 3D-яйцо и шейдеры тёмной стороны.
- **d3-geo + topojson-client** — векторная карта терруаров.
- **@resvg/resvg-js** — серверный рендер og-карточек результата теста «Какое вы яйцо?» (SVG → PNG, шрифты в `src/shared/assets/fonts`).
- Нативный Astro i18n (`ru`/`en`) + собственная утилита переводов, без клиентских i18n-библиотек.
- Архитектура — Feature-Sliced Design (`app / pages / widgets / features / shared`).

## Команды

| Команда           | Действие                                    |
| :---------------- | :------------------------------------------ |
| `npm ci`          | Установка зависимостей по lockfile          |
| `npm run dev`     | Dev-сервер на `localhost:4321` (с `--host`) |
| `npm run build`   | Сборка в `./dist/`                          |
| `npm run preview` | Локальный предпросмотр сборки               |
| `npm start`       | Собранный сайт и poller модерации           |
| `npm test`        | Проверки повторной доставки, ошибок и healthcheck |

Требуется Node.js >= 22.12.

## Переменные окружения

Смотри `.env.example`:

- `DOMAIN` — базовый домен (сайт живет на поддомене `egg.`).
- `NEWS_API_KEY` — ключ [NewsData.io](https://newsdata.io) для мировой яичной ленты в блоге. Без ключа лента пустая, сайт работает.
- `FONIN_ACCESS_WORD` — кодовое слово закрытой кладки подарков.
- `FONIN_GIFT_TOKEN_SECRET` — секрет подписи временных ссылок на сертификаты (если не задан — используется кодовое слово, задать настоятельно рекомендуется).
- `FONIN_GIFT_PRIVATE_DIR` — путь к директории с PNG-сертификатами (по умолчанию `private/fonin-gifts`).
- `KLADKA_BOT_TOKEN`, `KLADKA_ADMIN_CHAT_ID` — Telegram-бот премодерации Книги Кладки (`/kladka`): записи прилетают админу с кнопками «Одобрить/Отклонить». Ответы забирает встроенный long poller `scripts/kladka-poller.mjs` и передаёт локальному API; публичный Telegram webhook не используется. Без токена записи публикуются сразу. Опционально: `KLADKA_WEBHOOK_SECRET` (подпись внутреннего вызова poller → API), `KLADKA_IP_SALT`, `KLADKA_DATA_DIR`.

После изменения `.env` контейнер нужно **пересоздать**: обычный
`docker compose restart` не перечитывает `env_file`.

```sh
docker compose up -d --force-recreate web
```

## Деплой

Docker-first, без CI/CD:

```sh
docker compose up -d --build
```

`docker-compose.yml` рассчитан на внешнюю сеть `caddy_net` с Caddy в роли reverse proxy (лейблы для автоматического SSL уже прописаны). Кэш новостей живет в named volume `news_cache`, записи и фотографии Книги Кладки — в `kladka_data`; оба переживают redeploy.

Контейнер стартует через `scripts/start.mjs`: он одновременно запускает Astro
preview-сервер и long polling Telegram для кнопок модерации. Состояние Книги
по-прежнему изменяет только API-процесс приложения.

### Модерация и диагностика

Правки применены на big-one 06.09.2026: `egg-culture-web:release-20260906-audit`.
Проверены Docker healthcheck, свежий успешный опрос Telegram, публичные `/ru/`
и `/en/`, совпадение исходников poller с образом. Рабочие тома и хеш `posts.json`
после пересоздания сохранились. Для будущих правок по-прежнему нужны build/recreate.

- Poller подтверждает обновление новым `offset` только после успешной доставки
  локальному API. Сбой останавливает обработку остатка пакета; недоставленное
  обновление повторяется. Запись итогового статуса идемпотентна.
- Сетевые ошибки повторяются с паузами 5, 10, 20, 40, затем 60 секунд.
  Для HTTP 429 учитывается `retry_after`; при 409 проверяется наличие webhook,
  чтобы не удалять его повторно при конфликте двух poller-процессов.
- В лог попадают коды вроде `UND_ERR_CONNECT_TIMEOUT` или `LOCAL_HTTP_500`,
  без URL с токеном. Сообщение о восстановлении означает успешный цикл опроса
  и доставки, а не доказательство постоянной доступности Telegram.
- Healthcheck проверяет HTTP сайта и состояние poller в `/tmp`. Пять минут
  без успешного опроса или с заблокированной доставкой дают ошибку; Docker
  помечает контейнер `unhealthy` после трёх неудачных проверок. Сам статус
  `unhealthy` не перезапускает контейнер. Без настроенного бота проверяется сайт.
- Ротация stdout/stderr задана через Docker `json-file`: 10 МБ × 3 файла.

Новый healthcheck виден Docker и `bootstrap/check`; отдельный endpoint Egg
в Gatus пока не настроен, поэтому автоматического алерта Gatus на stale poller
ещё нет. Подключение записано отдельной задачей.

Это повторная доставка из очереди Telegram, не долговечная локальная очередь:
[Bot API хранит обновления не более 24 часов](https://core.telegram.org/bots/api#getupdates).
Длительный простой всё ещё может потребовать ручной модерации оставшихся записей.

В Compose сохраняется исходящий пин `api.telegram.org:149.154.167.220`; входящий
webhook не используется. На 05.09 старый poller записал 28 `fetch failed` за час,
хотя отдельный `getMe` прошёл и очередь Telegram была пуста. Исправление повторов
не устраняет сетевую причину. Новый poller выкачен 06.09; долговременную
стабильность маршрута нужно оценивать по свежим кодам ошибок и healthcheck.
В первые примерно четыре минуты после применения отмечены два
`UND_ERR_CONNECT_TIMEOUT` и одно сообщение о восстановлении; на контрольной
проверке опрос был свежим, контейнер `healthy`, перезапусков не было.
Таким образом, сетевые таймауты сохраняются, а повторный опрос восстанавливается.

Нативный `fetch` поддерживает proxy env в Node **22.21+** с явным
`NODE_USE_ENV_PROXY=1`, а не только в Node 24
([документация Node](https://nodejs.org/learn/http/enterprise-network-configuration)).
Прокси сейчас не настроен. Если его добавлять, локальный callback должен идти
мимо него: `NO_PROXY=localhost,127.0.0.1`.

При проверке lockfile 05.09 `npm audit` сообщил о 12 записях (9 high),
включая Astro и транзитивные зависимости. Сборка и тесты не устраняют эти
предупреждения; обновление зависимостей и оценка достижимости уязвимого кода
остаются отдельной задачей.

### Данные и резервные копии

Named volume переживает пересоздание контейнера, но не потерю хоста. Регулярного
offsite-бэкапа Egg Culture на 05.09 нет; его настройка отложена отдельной задачей.
Для восстановления нужны:

- весь `kladka_data`: записи, статусы модерации и загруженные фотографии;
- `private/fonin-gifts/*.png`: исходники приватных сертификатов вне Git,
  которые Docker копирует в образ при сборке;
- `.env` с ключами и кодовыми словами плюс исходники и lockfile из Git.

`news_cache` можно восстановить повторным получением новостей. Будущий бэкап
Книги должен согласованно сохранять JSON-состояние и фотографии; простая копия
тома во время записи не доказывает согласованность. Критерий готовности —
изолированное восстановление записей, статусов и файлов без отправки Telegram.
Общее покрытие описано в [BACKUPS.md](../../BACKUPS.md).

Прогресс Инкубатора и признак прохождения квиза находятся в `localStorage`
браузера, черновик заметки — в `sessionStorage`. Сервер их не получает,
поэтому серверный бэкап не восстановит это состояние на другом устройстве.

Приватные сертификаты кладки (`private/fonin-gifts/*.png`) не хранятся в git — при деплое из чистого клона их нужно положить на место руками, иначе API отдаст 404 (мягкая деградация).

## Структура

```text
src/
├── app/        # Layout, глобальные стили (дизайн-токены)
├── pages/      # Роутинг: [lang]/... (ru|en) + api/
├── widgets/    # Header, Footer, DescriptorWheel
├── features/   # EggCalculator, EggOracle, TerroirMap, Quiz,
│               # GiftVault, QiCompass, TastingNote, WorldNews,
│               # EggPersona, Incubator, AdeptCertificate, MotionTrials, KladkaBook
├── shared/     # i18n, утилиты (newsCache, foninGiftToken), UI, данные
└── data/       # Контент: блог (md), товары, события
```

## Откат применения 06.09.2026

На big-one сохранён `egg-culture-web:rollback-20260906-audit` и прежний Compose
в `/tmp/server-rollout-20260906-audit/egg-culture-rollback.json`.
Старый OCI-образ Docker уже не находил, поэтому образ отката собран из
файловой системы ещё работавшего контейнера. Параметры запуска восстановлены,
секретные container env в образ не переносились. Изолированный запуск и HTTP
проверены. Это образ приложения; рабочие named volumes в него не входят.

Временный каталог `/tmp` не является постоянным хранилищем аварийного runbook.
Откат изменяет работающий сервис и выполняется только в согласованном окне:

```sh
# На big-one, из каталога проекта
docker compose --project-name egg-culture --project-directory "$PWD" \
  -f /tmp/server-rollout-20260906-audit/egg-culture-rollback.json \
  up -d --no-deps --no-build --pull never web
```
